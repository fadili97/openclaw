[build]
builder = "dockerfile"

[deploy]
# PORT is read from env by the gateway (see resolveGatewayPort); --bind lan exposes the HTTP server.
startCommand = "node openclaw.mjs gateway --allow-unconfigured --bind lan"
healthcheckPath = "/__openclaw/control-ui-config.json"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[variables]
PORT = "8080"
NODE_ENV = "production"
OPENCLAW_PREFER_PNPM = "1"
OPENCLAW_STATE_DIR = "/data/.openclaw"
OPENCLAW_WORKSPACE_DIR = "/data/workspace"

# Browser automation (uncomment if you need Playwright/Puppeteer)
# OPENCLAW_INSTALL_BROWSER = "1"
